export const environment = {
  // Es la URL de la segunda capa de la arquitectura.
  production: false,
  apiUrl: 'http://localhost:4000/api' // URL base para todos los endpoints (ej: /api/citas)

};
